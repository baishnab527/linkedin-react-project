

// create initial state here;

const initialState = {
    linkedinPosts: [],
    error: null,
    loader: null, 
    message: false,
};


// export  default initialState;
export default initialState;